networks:
  invisionu-ats-network:
    driver: bridge

volumes:
  postgres_data:
  minio_data:
  # hf_cache volume of (aidetect) downloads a Hugging Face model
  hf_cache:
  # this go cache for Backend Golang Test
  go-mod-cache: {}
  go-build-cache: {}

services:
  frontend:
    build:
      context: ./frontend
      dockerfile: Dockerfile
      args:
        BACKEND_INTERNAL_URL: ${BACKEND_INTERNAL_URL:-http://backend:8080}
        NEXT_PUBLIC_API_URL: ${NEXT_PUBLIC_API_URL}
    container_name: invisionu_frontend
    restart: unless-stopped
    ports:
      - "${FRONTEND_HOST_PORT:-3000}:3000"
    env_file:
      - .env
    environment:
      NODE_ENV: production
      NEXT_PUBLIC_API_URL: ${NEXT_PUBLIC_API_URL}
    depends_on:
      backend:
        condition: service_healthy
    networks:
      - invisionu-ats-network

  postgres:
    image: postgres:17-alpine
    container_name: invisionu_postgres
    ports:
      - "5432:5432"
    restart: unless-stopped
    env_file:
      - .env
    environment:
      POSTGRES_DB: ${DB_DATABASE}
      POSTGRES_USER: ${DB_USER}
      POSTGRES_PASSWORD: ${DB_PASSWORD}
    volumes:
      - postgres_data:/var/lib/postgresql/data
    healthcheck:
      test: ["CMD-SHELL", "pg_isready -U ${DB_USER} -d ${DB_DATABASE}"]
      interval: 5s
      timeout: 3s
      retries: 20
    networks:
      - invisionu-ats-network

  backend:
    build:
      context: ./backend
      dockerfile: Dockerfile
      target: runner
    container_name: invisionu_backend
    env_file:
      - .env
    ports:
      - "${BACKEND_HOST_PORT:-8080}:8080"
    restart: unless-stopped
    command: ["-config=prod"]
    volumes:
      - ./backend/config:/app/config:ro
    depends_on:
      postgres:
        condition: service_healthy
      minio-init:
        condition: service_completed_successfully
    healthcheck:
      test: ["CMD-SHELL", "wget -qO- http://127.0.0.1:8080/health >/dev/null 2>&1 || exit 1"]
      interval: 10s
      timeout: 5s
      retries: 20
      start_period: 20s
    networks:
      - invisionu-ats-network

  # for Running Backend Tests:
  # docker compose run --rm --no-deps backend-test go test ./internal/healthcheck
  backend-test:
    build:
      context: ./backend
      dockerfile: Dockerfile
      target: test
    working_dir: /src
    env_file:
      - .env
    volumes:
      - .:/src
      - go-mod-cache:/go/pkg/mod
      - go-build-cache:/root/.cache/go-build
    command: ["sh"]
    networks:
      - invisionu-ats-network

  # s3 minio storage bucket
  minio:
    image: quay.io/minio/minio:RELEASE.2025-02-28T09-55-16Z
    container_name: invisionu_s3_minio
    restart: unless-stopped
    environment:
      MINIO_ROOT_USER: ${STORAGE_ACCESS_KEY}
      MINIO_ROOT_PASSWORD: ${STORAGE_SECRET_KEY}
    command: server /data --console-address ":9001"
    volumes:
      - minio_data:/data
    ports:
      - "9000:9000"
      - "9002:9001"
    healthcheck:
      test: ["CMD-SHELL", "curl -fsS http://127.0.0.1:9000/minio/health/ready >/dev/null || exit 1"]
      interval: 5s
      timeout: 3s
      retries: 20
    networks:
      - invisionu-ats-network

  # create/configure s3 minio bucket for local dev
  minio-init:
    image: quay.io/minio/mc:RELEASE.2025-02-21T16-00-46Z
    container_name: invisionu_s3_minio_init
    depends_on:
      minio:
        condition: service_healthy
    restart: "no"
    entrypoint: >
      /bin/sh -ec "
      until /usr/bin/mc alias set local http://minio:9000 ${STORAGE_ACCESS_KEY} ${STORAGE_SECRET_KEY}; do
        echo 'waiting for minio...';
        sleep 2;
      done;
      /usr/bin/mc mb --ignore-existing local/${STORAGE_BUCKET:-application-files};
      /usr/bin/mc anonymous set private local/${STORAGE_BUCKET:-application-files};
      "
    networks:
      - invisionu-ats-network

  llmscoring:
    build:
      context: ./llm
      dockerfile: Dockerfile
    container_name: invisionu_llscoring
    ports:
      - "9094:9094"
    env_file:
      - .env
    restart: unless-stopped
    healthcheck:
      test: ["CMD-SHELL", "curl -fsS http://127.0.0.1:9094/docs >/dev/null || exit 1"]
      interval: 10s
      timeout: 5s
      retries: 10
    networks:
      - invisionu-ats-network

  sttwhisper:
    build:
      context: ./STT
      dockerfile: Dockerfile
    container_name: invisionu_sttwhisper
    ports:
      - "9095:9095"
    env_file:
      - .env
    restart: unless-stopped
    healthcheck:
      test: ["CMD-SHELL", "curl -fsS http://127.0.0.1:9095/docs >/dev/null || exit 1"]
      interval: 10s
      timeout: 5s
      retries: 10
    networks:
      - invisionu-ats-network

  aidetect:
    build:
      context: ./AI_Detecter-service
      dockerfile: Dockerfile
    container_name: invisionu_aidetect
    volumes:
      - hf_cache:/root/.cache/huggingface
    ports:
      - "9873:9873"
    healthcheck:
      test: ["CMD", "curl", "-f", "http://localhost:9873/health"]
      interval: 30s
      timeout: 10s
      retries: 4
    restart: unless-stopped
    networks:
      - invisionu-ats-network

  parserapi:
    build:
      context: ./parser
      dockerfile: Dockerfile
    container_name: invisionu_parserapi
    ports:
      - "8002:8001"
    healthcheck:
      test: ["CMD", "curl", "-f", "http://localhost:8001/health"]
      interval: 30s
      timeout: 10s
      retries: 4
    restart: unless-stopped
    networks:
      - invisionu-ats-network
