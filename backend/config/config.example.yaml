# =========================
# Backend
# =========================

server:
  http_port: 8080
  http_host: "0.0.0.0"

database:
  host: "localhost"
  port: 5432
  user: "myuser"
  password: "password"
  name: "mydb"
  sslmode: "disable"

# development/staging/production
environment: "development"

gotenberg:
  url: "http://localhost:3000"

auth:
  jwt_access_secret: "change-me"
  jwt_refresh_secret: "change-me-too"
  access_token_ttl_seconds: 3600
  refresh_token_ttl_seconds: 604800
  email_verification_code_ttl_seconds: 900

storage:
  provider: "s3"
  endpoint: "minio:9000"
  region: "us-east-1"
  bucket: "application-files"
  access_key: "minioadmin"
  secret_key: "minioadmin"
  use_ssl: false

messaging:
  enabled: false
  mode: "stub"
  url: "amqp://guest:guest@rabbitmq:5672/"
  exchange: "invisionu.events"
  application_submitted_key: "application.submitted"

email:
  enabled: false
  mode: "stub"  # stub for dev, smtp for prod
  from_name: "InVisionU ATS"
  from_email: "noreply@example.com"
  smtp_host: "smtp.example.com"
  smtp_port: 587
  smtp_user: "jokergame434@gmail.com"
  smtp_pass: "mfkb wimn pthi dnhf"

llm: # TODO: (refactor) this should be llmscoring
  enabled: false

llm_assessment:
  provider: "openai"
  base_url: "https://api.openai.com/v1"
  api_key: ""
  question_model: "gpt-4o-mini"
  evaluation_model: "gpt-4o-mini"
  request_timeout_seconds: 30

assessment:
  timeout_minutes: 15

screening:
  stt_base_url: "http://sttwhisper:9095"
  llm_base_url: "http://llmscoring:9094"
  request_timeout_seconds: 180
  presign_ttl_seconds: 900

# =========================
# Modules and Services on internal/
# =========================


# =========================
# OBSERVABILITY SETTINGS
# =========================

# Logging configuration
logging:
  level: "debug"           # debug, info, warn, error
  format: "console"        # json (production) or console (development)
  output: "stdout"         # stdout, stderr, or file path
  time_format: "2006-01-02T15:04:05Z07:00"
  sensitive_paths:         # Endpoints with sensitive data - body_preview will be <redacted>
    - "/auth/register"
    - "/auth/login"
    - "/auth/refresh"
    - "/auth/verify-email"
    - "/docs"

# =========================
# SECURITY SETTINGS
# =========================

security:
  # CORS settings
  allowed_origins:
    - "http://localhost:3000"
    - "http://localhost:8080"
    - "*"                    # Allow all for development (restrict in production!)
  allowed_methods:
    - "GET"
    - "POST"
    - "PUT"
    - "DELETE"
    - "PATCH"
    - "HEAD"
    - "OPTIONS"
  allowed_headers:
    - "Origin"
    - "Content-Type"
    - "Accept"
    - "Authorization"
    - "X-Request-ID"
    - "X-Correlation-ID"
  expose_headers:
    - "X-Request-ID"
    - "X-Correlation-ID"
  allow_credentials: false
  max_age: 300             # Preflight cache duration in seconds
  rate_limit_max: 100      # Maximum requests per window
  rate_limit_window_seconds: 60  # Time window in seconds
  request_timeout_seconds: 30
  trusted_proxies:
    - "127.0.0.1"
